# 分支整理与功能保存（2026-09-15）

整理前已执行 `git fetch origin`，远端与本地已知功能分支一致。以下分支沿同一条历史前进，没有独立分叉，不需要冲突合并：

| 分支 | 整理前提交 | 内容 |
|---|---|---|
| main | 24e929f | 初始工程 |
| feature/pca9685-servo-library | 204c45b | PCA9685 舵机封装及系统设计 |
| features/mechanical | 6be3ac1 | RevC 总装与打印组织 |
| features/simulator | 6eb8de9 | RevC 上位机工作台 |
| release/windows-packaging | c9d2371 | Windows 打包与原版备份 |
| feature/live-tcp-reachability | d76602c | 实时末端试摆、手动控制、浅色工作台 |
| feature/command-console | 本轮新增 | 0.4.0 控制台、CLI、SDK 与通信设计 |

main 先快进到 d76602c，控制台在 feature/command-console 开发，验证后再快进整合到 main。历史功能分支与 `backup/pre-live-tcp-20260915` 标签均保留，便于查找各阶段。未执行 rebase、强推、删除分支或清理未跟踪文件。

工作区原有机械资料、机械脚本、Blender/Fusion 工具和 VS Code 配置改动保留原位，不纳入上位机提交；这些未提交资料仅存在本地，不能把“上位机功能已保存”理解为这些资料也已进入 Git。后续机械任务应自行筛选并提交它们。

Windows 新版目录为 `desktop/dist/console/win-unpacked`，此前 `win-unpacked`、`live-tcp`、`workbench` 目录保留。发布产物和测试日志沿用 `.gitignore`，源码、测试、锁文件和相关说明进入 Git。
