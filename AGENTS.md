# CyberArm 项目约定

- 分支名称不得包含 `codex`（不区分大小写）。新功能分支使用 `feature/`，修复使用 `fix/`。
- 上位机手动控制默认实时显示当前仿真姿态，路径预览默认关闭；用户显式开启预览或进入示教、自动模式时才展示规划路径。
- 上位机界面采用浅色工业工作台风格：白灰底色、蓝色强调、固定控制区，说明文字集中放置。
- 工作区可能包含未提交的机械设计资料；上位机任务只提交自身代码、测试和相关文档，保留无关改动。
